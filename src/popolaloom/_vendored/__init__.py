"""Vendored third-party modules — see :doc:`/VENDORING.md` at repo root.

PopolaLoom v0.5.0 (Stage S1, locked decision D5.7 Path B) vendors a
minimal subset of `ArkTower <https://github.com/YoRHa-Agents/ArkTower>`_
into ``popolaloom._vendored.arktower`` so that ``pip install popolaloom``
succeeds on a fresh machine without requiring a separate ArkTower clone.

Refresh procedure: ``VENDORING.md`` at the repo root.
"""
